# Reviewing docs PRs

- [ ] frontmatter `title` / `description` 合理（无「存放所有日志…」占位）
- [ ] 无指向本仓不存在文件的相对链接
- [ ] meta 权威指针格式正确
- [ ] 操作类页面优先用 `<Steps>`
- [ ] `npm run check` 与 `npm run build` 通过
- [ ] 未引入生产密钥明文
