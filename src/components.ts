/**
 * Nimbus expects `src/components.ts` exporting a `components` map for MDX.
 * Keep in sync with `src/mdx-components.ts`.
 */
export { components } from './mdx-components';
export * from './mdx-components';
